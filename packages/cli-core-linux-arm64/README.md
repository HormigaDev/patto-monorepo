# @patto/cli-core-linux-arm64

Native statically linked `patto-core` binary for Linux arm64, built with the `aarch64-unknown-linux-musl` Rust target.

This package is installed as an optional dependency of `@patto/cli` only on matching platforms.
