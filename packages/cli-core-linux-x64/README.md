# @patto/cli-core-linux-x64

Native statically linked `patto-core` binary for Linux x64, built with the `x86_64-unknown-linux-musl` Rust target.

This package is installed as an optional dependency of `@patto/cli` only on matching platforms.
